<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>These are all of our recipes - grandma's got nothing on this!</title>
  </head>
  <body>
    <% @recipes.each do |recipe| %>
    <%= recipe.name %>
    <%= recipe.ingredients %>
    <%= recipe.cook_time %>
    <a href=<%= "/recipes/#{recipe.id}" %>>Recipe Page</a>
    <% end %>
  </body>
</html>
