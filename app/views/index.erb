<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>These are all of our recipes - your grandma can't cook</title>
  </head>
  <body>
    <% @recipes.each do |recipe| %>
    <%= recipe.name %>
    <%= recipe.ingredients %>
    <%= recipe.cook_time %>
    <a href=<%= "/recipes/#{recipe.id}" %>>Recipe Page</a>
    <% end %>
  </body>
</html>
